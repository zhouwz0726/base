
    工厂模式
        定义一个创建对象的接口，让其子类自己去决定实例化那个一个类，工程模式可以使其创建过程延迟到子类进行
        示例：
                1:定义一个Shape接口
                2:定义实体类(Square,Circle)去实现Shape接口，并重写draw方法
                3:定义一个工厂类(ShapeFactory)
                4:demo中是使用工厂类去实例化需要示例的对象
