

        对象锁：
            一般加载非静态方法，代码块等等都是对象锁，对象锁，顾名思义就是进入该对象的时候获取对应的对象锁，每个对象都有自己对应的对象锁，不同对象之前的锁互不影响。

        类锁：
            一般加载静态方法的锁(当然也可以直接synchronized对应的class)，为类锁，类锁，在JVM中只要一个类，并存放在JVM的方法区，属于线程共享，故进入需要获取这个类对应的类锁。

            备注：类锁和对象锁是两个不同的锁，控制着不同的区域，互不干扰，线程获取了对象锁的同时，也可以获取类锁
